---
import {faqOrder} from "@assets/constants.astro"
import {faqPrice} from '@assets/constants.astro'
import {faqSite} from '@assets/constants.astro'
import {faqOther} from '@assets/constants.astro'
---
<div class="w-full bg-slate-200 mt-16">
  <p id="faq">お客様からのご質問にお答えします</p>
  <p>納期/ご依頼内容などについて</p>
{faqOrder.map((faq)=> (
  <div x-data="{activeAccordion: '', setActiveAccordion(id) {this.activeAccordion = (this.activeAccordion == id) ? '' : id}}"
   class="relative w-11/12 mx-auto overflow-hidden text-sm font-normal bg-white border border-gray-200 divide-y divide-gray-200 rounded-md"
    >
    <div x-data="{ id: $id('accordion') }" class="cursor-pointer group">
      <button
        @click="setActiveAccordion(id)"
        class="flex items-center justify-between w-90 p-4 text-left select-none group-hover:underline">
        <span>{faq.question}</span>
        <svg
          class="w-4 h-4 duration-200 ease-out"
          :class="{ 'rotate-180': activeAccordion==id }"
          viewBox="0 0 24 24"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          >
            <polyline points="6 9 12 15 18 9"></polyline></svg>
      </button>
      <div x-show="activeAccordion==id" x-collapse x-cloak>
        {faq.answer}
        <div class="p-4 pt-0 opacity-70">
        </div>
      </div>
    </div>
  </div>
))}
<p>費用について</p>
{faqPrice.map((faq)=> (
  <div x-data="{activeAccordion: '', setActiveAccordion(id) {this.activeAccordion = (this.activeAccordion == id) ? '' : id}}"
   class="relative w-11/12 mx-auto overflow-hidden text-sm font-normal bg-white border border-gray-200 divide-y divide-gray-200 rounded-md"
    >
    <div x-data="{ id: $id('accordion') }" class="cursor-pointer group">
      <button
        @click="setActiveAccordion(id)"
        class="flex items-center justify-between w-90 p-4 text-left select-none group-hover:underline">
        <span>{faq.question}</span>
        <svg
          class="w-4 h-4 duration-200 ease-out"
          :class="{ 'rotate-180': activeAccordion==id }"
          viewBox="0 0 24 24"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          >
            <polyline points="6 9 12 15 18 9"></polyline></svg>
      </button>
      <div x-show="activeAccordion==id" x-collapse x-cloak>
        {faq.answer}
        <div class="p-4 pt-0 opacity-70">
        </div>
      </div>
    </div>
  </div>
))}
<p>制作/公開環境などについて</p>
<p>その他</p>
  </div>
